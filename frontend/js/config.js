/* ==========================================================================
   GIULIVO — configurazione condivisa
   Un solo posto da aggiornare con l'URL reale del backend dopo il deploy su
   Render (es. 'https://giulivo-backend.onrender.com/api').
   ========================================================================== */
const API_BASE_URL = 'https://giulivo-backend.onrender.com/api';
